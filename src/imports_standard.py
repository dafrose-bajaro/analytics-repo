# standard imports
from glob import glob #for using different wildcards to retrieve files
import matplotlib.pyplot as plt #used for basic graphs and plots
import pandas as pd #working on dataframes
import json #working on and parsing json data
import gdal #working with both gdal (raster) and ogr (vector) data, python bindings from c++
import numpy as np #working with np arrays
import os #working with operating system    
import requests #working with http requests
import warnings #prevents specific warnings from showing
import yaml #working on yaml files

# plotting
import matplotlib.pyplot as plt #used for basic graphs and plots
import seaborn as sns #used for more complex graphs and plots
import plotly.express as px #used for interactive graphs and plots
import plotly.graph_objects as go #used for interactive graphs and plots